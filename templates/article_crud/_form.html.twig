{{ form_start(form) }}
<div class="space-y-4">
    {{ form_widget(form) }}
    <button class="w-full bg-green-500 text-white py-2 rounded-lg shadow hover:bg-green-600 transition">
        {{ button_label|default('Enregistrer') }}
    </button>
</div>
{{ form_end(form) }}
